apiVersion: v1
kind: Service
metadata:
  name: {{ include "infraforge-ui.fullname" . }}
  labels:
    {{- include "infraforge-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "infraforge-ui.selectorLabels" . | nindent 4 }}
